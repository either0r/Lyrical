<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Lyrical.Pages.SongListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Lyrical.Models"
    xmlns:converters="using:Lyrical.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:CreatorToBrushConverter x:Key="CreatorToBrushConverter" />
    </Page.Resources>

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            
            <TextBlock
                Grid.Column="0"
                Text="My Songs"
                Style="{StaticResource TitleTextBlockStyle}" />

            <Image
                Grid.Column="1"
                Source="/Assets/Images/Logo.svg"
                Height="48" />

            <Button
                Grid.Column="2"
                Content="Open File"
                Click="OpenFileButton_Click" />

            <Button
                Grid.Column="3"
                Content="New Song"
                Click="NewSongButton_Click" />
        </Grid>

        <ListView
            x:Name="SongListView"
            Grid.Row="1"
            ItemsSource="{x:Bind Songs}"
            IsItemClickEnabled="True"
            SelectionMode="Single"
            ItemClick="SongListView_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:SongDocument">
                    <Border
                        Padding="8"
                        CornerRadius="8"
                        Margin="0,0,0,8"
                        Background="{Binding CreatedBy, Converter={StaticResource CreatorToBrushConverter}}">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="{x:Bind Title}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <TextBlock Grid.Row="1" Text="{x:Bind CreatedBy}" Opacity="0.75" />
                                <TextBlock Grid.Row="2" Text="{x:Bind LastModifiedText}" Opacity="0.6" />
                            </Grid>

                            <Button
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Click="DeleteSongButton_Click"
                                CommandParameter="{x:Bind}">
                                <SymbolIcon Symbol="Delete" />
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
